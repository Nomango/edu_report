import Zmage from "react-zmage";

function Image(props) {
    return (
        <Zmage {...props}
            // controller={false}
            // hotKey={false}
        />
    )
}

export default Image